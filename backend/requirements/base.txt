django>=5.0,<5.1
djangorestframework>=3.15,<3.16
django-cors-headers>=4.4,<5
django-filter>=24.3,<25
djangorestframework-simplejwt>=5.3,<6
dj-database-url>=2.2,<3
psycopg2-binary>=2.9,<3
redis>=5.1,<6
celery>=5.4,<6
django-celery-beat>=2.6,<3
django-celery-results>=2.5,<6
python-json-logger>=2.0,<3
cryptography>=43.0,<44
pydantic>=2.9,<3
gunicorn>=22.0,<23
httpx>=0.27,<0.28

# ---------------------------------------------------------------------------
# Runtime dependencies imported by application code but previously declared in
# NO requirements file (they had to be hand-installed into the local venv).
# Pinned to the versions the codebase was validated against. Keep pinned:
#   - django-prometheus 2.5.0+ requires Django!=5.0.*,<6.1 (excludes the pinned
#     Django 5.0.x line), so it must stay on <2.5.
#   - requests is imported by the zerodha/chartink/fred/marketaux providers.
# ---------------------------------------------------------------------------
django-prometheus==2.4.0
python-decouple==3.8
structlog==24.4.0
channels==4.3.2
channels-redis==4.3.0
Jinja2==3.1.6
requests==2.34.2

# ---------------------------------------------------------------------------
# ASGI server — the backend container runs `daphne config.asgi:application`
# (infra/docker/backend/Dockerfile.dev CMD + infra/docker-compose.yml), and
# the entrypoint `exec`s it. Declared here so a fresh image build can boot;
# previously only channels was present and daphne was never installed.
# ---------------------------------------------------------------------------
daphne==4.1.2
